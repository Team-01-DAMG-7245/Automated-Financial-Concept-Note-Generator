# --- FastAPI & server ---
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6
orjson==3.10.7            # optional, faster JSON for FastAPI
gunicorn==21.2.0          # prod server (if you deploy)

# --- Vector stores ---
pinecone==6.0.0           # new SDK (not pinecone-client)
chromadb==0.4.22

# --- LLM & embeddings ---
openai>=1.10.0,<2.0       # matches your current client, works with Instructor
instructor>=1.3.0         # pydantic v2-compatible structured output
sentence-transformers==2.3.1
langchain==0.1.5
langchain-openai==0.0.3

# --- Persistence (Postgres cache) ---
SQLAlchemy>=2.0,<2.1
psycopg
alembic>=1.12.0

# --- Retrieval helpers / utils ---
python-dotenv==1.0.0
httpx==0.26.0
aiofiles==23.2.1
wikipedia==1.4.0

# --- Dev / testing ---
pytest==7.4.4
pytest-asyncio==0.23.3
black==24.1.1
flake8==7.0.0
